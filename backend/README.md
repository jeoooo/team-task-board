# Team Task Board: Backend

NestJS + Prisma REST API. See the [root README](../README.md) for full setup instructions, the ER diagram, and the decisions/tradeoffs write-up.

Quick start:

```bash
npm install
npx prisma migrate dev
npx prisma db seed
npm run start:dev
```

API runs on `http://localhost:3001`.

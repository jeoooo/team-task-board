# Team Task Board: Frontend

React + Redux Toolkit + MUI client. See the [root README](../README.md) for full setup instructions.

Quick start:

```bash
npm install
npm run dev
```

Runs on `http://localhost:5173` and expects the backend at `http://localhost:3001` (see `.env`).
